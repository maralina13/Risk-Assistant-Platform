{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "notification_sent.schema.json",
  "title": "NotificationSent",
  "type": "object",
  "required": ["event_id", "event_type", "correlation_id", "created_at", "version", "payload"],
  "properties": {
    "event_id": { "type": "string" },
    "event_type": { "const": "NotificationSent" },
    "correlation_id": { "type": "string" },
    "created_at": { "type": "string" },
    "version": { "type": "string" },
    "payload": {
      "type": "object",
      "required": ["task_id", "channel", "status"],
      "properties": {
        "task_id": { "type": "string" },
        "channel": { "type": "string" },
        "status": { "type": "string" }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
